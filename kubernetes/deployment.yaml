apiVersion: apps/v1
kind: Deployment
metadata:
    name: wallet-app

spec:
    replicas: 1

    selector:
        matchLabels:
            app: wallet-app

    template:
        metadata:
            labels:
                app: wallet-app

        spec:
            containers:
                - name: wallet-app
                  image: alialrabi/e-wallet:latest

                  ports:
                      - containerPort: 8091

                  envFrom:

                      # Non-secret configuration
                      - configMapRef:
                            name: app-config

                      # Passwords and credentials
                      - secretRef:
                            name: app-secrets